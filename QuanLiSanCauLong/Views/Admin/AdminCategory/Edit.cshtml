@model QuanLiSanCauLong.Models.ProductCategory

@{
    ViewData["Title"] = "Sửa danh mục";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid py-5 min-vh-100 detail-page-bg">
    <div class="row justify-content-center">
        <div class="col-lg-10">
            <div class="mb-4 d-flex justify-content-between align-items-center">
                <a asp-action="Index" class="text-decoration-none text-secondary fw-bold small">
                    <i class="bi bi-chevron-left me-1"></i> HỦY THAY ĐỔI
                </a>
                <span class="text-muted small fw-bold">ID: #@Model.CategoryId</span>
            </div>

            <div class="card border-0 shadow-lg rounded-5 overflow-hidden glass-card">
                <div class="row g-0">
                    <div class="col-md-5 p-5 text-white bg-gradient-warning d-flex flex-column align-items-center justify-content-center text-center">
                        <div class="icon-box shadow-lg mb-4 text-warning" id="iconContainer">
                            <i class="bi @(Model.CategoryType == "Food" ? "bi-egg-fried" : Model.CategoryType == "Beverage" ? "bi-cup-straw" : "bi-gear-wide-connected") fs-1" id="previewIcon"></i>
                        </div>
                        <h2 class="fw-bold mb-1" id="previewName">@Model.CategoryName</h2>
                        <span class="badge bg-dark bg-opacity-25 rounded-pill px-3 py-2 text-uppercase mb-4" id="previewType">@Model.CategoryType</span>

                        <div class="alert alert-light bg-white bg-opacity-10 border-0 rounded-4 text-white small mt-4">
                            <i class="bi bi-info-circle me-2"></i> Bạn đang cập nhật thông tin cho danh mục này. Hãy đảm bảo các dữ liệu là chính xác.
                        </div>
                    </div>

                    <div class="col-md-7 p-5 bg-white bg-opacity-75 backdrop-blur">
                        <div class="d-flex justify-content-between align-items-center mb-4">
                            <h3 class="fw-bold text-dark mb-0">Cập Nhật</h3>
                            <div class="bg-warning bg-opacity-10 text-warning rounded-circle p-2">
                                <i class="bi bi-pencil-fill fs-4"></i>
                            </div>
                        </div>

                        <form asp-action="Edit" method="post">
                            <input type="hidden" asp-for="CategoryId" />

                            <div class="mb-4">
                                <label asp-for="CategoryName" class="form-label fw-bold small text-muted">TÊN DANH MỤC</label>
                                <input asp-for="CategoryName" id="inputName" class="form-control border-0 bg-light rounded-4 p-3 shadow-sm focus-ring-warning" />
                                <span asp-validation-for="CategoryName" class="text-danger small"></span>
                            </div>

                            <div class="mb-4">
                                <label asp-for="CategoryType" class="form-label fw-bold small text-muted">LOẠI DANH MỤC</label>
                                <select asp-for="CategoryType" id="inputType" class="form-select border-0 bg-light rounded-4 p-3 shadow-sm">
                                    <option value="Food">Đồ ăn</option>
                                    <option value="Beverage">Thức uống</option>
                                    <option value="Equipment">Dụng cụ</option>
                                </select>
                            </div>

                            <div class="mb-5">
                                <label asp-for="Description" class="form-label fw-bold small text-muted">MÔ TẢ</label>
                                <textarea asp-for="Description" id="inputDesc" class="form-control border-0 bg-light rounded-4 p-3 shadow-sm" rows="3">@Model.Description</textarea>
                            </div>

                            <button type="submit" class="btn btn-warning text-white rounded-pill w-100 py-3 fw-bold shadow-lg mt-2">
                                <i class="bi bi-cloud-check me-2"></i>LƯU THAY ĐỔI NGAY
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>