@model QuanLiSanCauLong.Models.BlogPost
@{
    Layout = null;
}

<style>
    :root {
        --deep: #1a1209;
        --gold: #d4a017;
        --gold2: #f5c842;
        --cream: #fdf8f0;
        --warm: #f5ede0;
        --border: #e8ddd0;
        --muted: #8a7560;
    }

    .cf-modal-header {
        padding: 1.4rem 1.75rem;
        border-bottom: 1px solid var(--border);
        background: var(--warm);
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

        .cf-modal-header h5 {
            font-family: 'Playfair Display',serif;
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--deep);
            margin: 0;
        }

        .cf-modal-header p {
            font-size: .8rem;
            color: var(--muted);
            margin: .2rem 0 0;
        }

    .cf-close-btn {
        background: none;
        border: none;
        color: var(--muted);
        cursor: pointer;
        font-size: 1.1rem;
        padding: .3rem;
        transition: color .2s;
        line-height: 1;
    }

        .cf-close-btn:hover {
            color: var(--deep);
        }

    .detail-section {
        margin-bottom: 1rem;
    }

    .cf-card {
        background: #fff;
        border: 1px solid var(--border);
        border-radius: 12px;
        overflow: hidden;
        margin-bottom: 1rem;
    }

    .cf-card-head {
        padding: .85rem 1.4rem;
        border-bottom: 1px solid var(--border);
        background: var(--warm);
        display: flex;
        align-items: center;
        gap: .5rem;
    }

    .cf-card-title {
        font-size: .7rem;
        font-weight: 700;
        letter-spacing: .14em;
        text-transform: uppercase;
        color: var(--muted);
    }

    .cf-card-icon {
        color: var(--gold);
        font-size: .9rem;
    }

    .cf-card-body {
        padding: 1.4rem;
    }

    /* CHIPS */
    .chip {
        display: inline-flex;
        align-items: center;
        gap: .3rem;
        font-size: .7rem;
        font-weight: 700;
        padding: .3rem .7rem;
        border-radius: 4px;
        letter-spacing: .04em;
    }

    .chip-published {
        background: rgba(22,163,74,.1);
        color: #16a34a;
    }

    .chip-draft {
        background: rgba(212,160,23,.12);
        color: #92660a;
    }

    .chip-archived {
        background: rgba(201,75,42,.1);
        color: #c94b2a;
    }

    .chip-category {
        background: rgba(79,70,229,.08);
        color: #4f46e5;
    }

    .chip-featured {
        background: rgba(212,160,23,.15);
        color: #92660a;
    }

    .chip-tag {
        background: var(--warm);
        color: var(--muted);
        border: 1px solid var(--border);
    }

    /* STAT ROW */
    .stat-row {
        display: grid;
        grid-template-columns: repeat(4,1fr);
        gap: 1px;
        background: var(--border);
        border: 1px solid var(--border);
        border-radius: 8px;
        overflow: hidden;
    }

    .stat-cell {
        background: var(--cream);
        padding: .8rem 1rem;
    }

    .stat-cell-label {
        font-size: .68rem;
        text-transform: uppercase;
        letter-spacing: .08em;
        color: var(--muted);
        margin-bottom: .2rem;
    }

    .stat-cell-val {
        font-size: .9rem;
        font-weight: 700;
        color: var(--deep);
    }

    /* POST TITLE */
    .post-title {
        font-family: 'Playfair Display',serif;
        font-size: 1.4rem;
        font-weight: 700;
        color: var(--deep);
        line-height: 1.3;
        margin-bottom: .75rem;
    }

    /* FEATURED IMG */
    .featured-img-wrap {
        border-radius: 10px;
        overflow: hidden;
        border: 1px solid var(--border);
        margin-bottom: 1rem;
    }

        .featured-img-wrap img {
            width: 100%;
            max-height: 320px;
            object-fit: cover;
            display: block;
        }

    /* EXCERPT BOX */
    .excerpt-box {
        background: var(--warm);
        border: 1px solid var(--border);
        border-left: 3px solid var(--gold);
        border-radius: 0 8px 8px 0;
        padding: 1rem 1.25rem;
        font-size: .88rem;
        color: var(--muted);
        font-style: italic;
        line-height: 1.6;
    }

    /* CONTENT */
    .post-content-box {
        border: 1px solid var(--border);
        border-radius: 8px;
        padding: 1.25rem;
        max-height: 380px;
        overflow-y: auto;
        font-size: .88rem;
        line-height: 1.7;
        color: var(--deep);
    }

        .post-content-box img {
            max-width: 100%;
            height: auto;
            border-radius: 6px;
            margin: 1rem 0;
        }

    /* SEO */
    .seo-row {
        display: grid;
        gap: .75rem;
    }

    .seo-field {
    }

    .seo-field-label {
        font-size: .68rem;
        text-transform: uppercase;
        letter-spacing: .08em;
        color: var(--muted);
        margin-bottom: .3rem;
        display: flex;
        align-items: center;
        gap: .4rem;
    }

    .seo-field-val {
        font-size: .85rem;
        color: var(--deep);
        padding: .6rem .9rem;
        background: var(--cream);
        border: 1px solid var(--border);
        border-radius: 6px;
    }

    /* ACTIONS */
    .cf-actions {
        display: flex;
        gap: 1rem;
        padding: 1.2rem 1.75rem;
        border-top: 1px solid var(--border);
        background: var(--warm);
        justify-content: flex-end;
    }

    .btn-primary {
        background: linear-gradient(135deg,var(--gold),var(--gold2));
        color: var(--deep);
        border: none;
        padding: .8rem 1.75rem;
        font-family: 'Outfit',sans-serif;
        font-size: .88rem;
        font-weight: 700;
        border-radius: 8px;
        cursor: pointer;
        display: flex;
        align-items: center;
        gap: .5rem;
        transition: all .25s;
    }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 5px 16px rgba(212,160,23,.35);
        }

    .btn-secondary {
        padding: .8rem 1.5rem;
        background: #fff;
        border: 1.5px solid var(--border);
        color: var(--muted);
        font-family: 'Outfit',sans-serif;
        font-size: .87rem;
        font-weight: 600;
        border-radius: 8px;
        cursor: pointer;
        transition: all .2s;
    }

        .btn-secondary:hover {
            border-color: var(--muted);
            color: var(--deep);
        }
</style>

<!-- MODAL HEADER -->
<div class="cf-modal-header">
    <div>
        <h5>Chi Tiết <em style="font-style:italic;color:var(--gold);">Bài Viết</em></h5>
        <p>Xem thông tin đầy đủ bài viết</p>
    </div>
    <button type="button" class="cf-close-btn" data-bs-dismiss="modal">
        <i class="bi bi-x-lg"></i>
    </button>
</div>

<div class="modal-body" style="padding:1.5rem;background:var(--cream);">

    <!-- STAT ROW -->
    <div class="stat-row" style="margin-bottom:1rem;">
        <div class="stat-cell">
            <div class="stat-cell-label"><i class="bi bi-calendar-plus" style="color:var(--gold);margin-right:.3rem;"></i>Ngày Tạo</div>
            <div class="stat-cell-val">@Model.CreatedAt.ToString("dd/MM/yyyy")</div>
        </div>
        @if (Model.PublishedAt.HasValue)
        {
            <div class="stat-cell">
                <div class="stat-cell-label"><i class="bi bi-calendar-check" style="color:#16a34a;margin-right:.3rem;"></i>Xuất Bản</div>
                <div class="stat-cell-val">@Model.PublishedAt.Value.ToString("dd/MM/yyyy")</div>
            </div>
        }
        <div class="stat-cell">
            <div class="stat-cell-label"><i class="bi bi-eye" style="color:#4f46e5;margin-right:.3rem;"></i>Lượt Xem</div>
            <div class="stat-cell-val">@Model.ViewCount.ToString("N0")</div>
        </div>
        <div class="stat-cell">
            <div class="stat-cell-label"><i class="bi bi-person" style="color:var(--muted);margin-right:.3rem;"></i>Tác Giả</div>
            <div class="stat-cell-val">@Model.AuthorId</div>
        </div>
    </div>

    <!-- TIÊU ĐỀ & BADGES -->
    <div class="cf-card">
        <div class="cf-card-head">
            <i class="bi bi-card-heading cf-card-icon"></i>
            <span class="cf-card-title">Thông Tin Bài Viết</span>
        </div>
        <div class="cf-card-body">
            @if (!string.IsNullOrEmpty(Model.FeaturedImage))
            {
                <div class="featured-img-wrap">
                    <img src="@Model.FeaturedImage" alt="@Model.Title" />
                </div>
            }

            <div class="post-title">@Model.Title</div>

            <div style="display:flex;flex-wrap:wrap;gap:.4rem;margin-bottom:1rem;">
                @switch (Model.Status)
                {
                    case "Published":
                        <span class="chip chip-published">✓ Đã Xuất Bản</span>
                        break;
                    case "Draft":
                        <span class="chip chip-draft">● Bản Nháp</span>
                        break;
                    default:
                        <span class="chip chip-archived">⊘ Lưu Trữ</span>
                        break;
                }
                @if (Model.Category != null)
                {
                    <span class="chip chip-category"><i class="bi bi-folder2"></i> @Model.Category.CategoryName</span>
                }
                @if (Model.IsFeatured)
                {
                    <span class="chip chip-featured">⭐ Nổi Bật</span>
                }
                @if (Model.AllowComments)
                {
                    <span class="chip" style="background:rgba(6,182,212,.08);color:#0e7490;">💬 Bình luận</span>
                }
            </div>

            @if (!string.IsNullOrEmpty(Model.Excerpt))
            {
                <div class="excerpt-box">@Model.Excerpt</div>
            }

            @if (!string.IsNullOrEmpty(Model.Tags))
            {
                <div style="display:flex;flex-wrap:wrap;gap:.4rem;margin-top:.75rem;">
                    @foreach (var tag in Model.Tags.Split(','))
                    {
                        <span class="chip chip-tag">#@tag.Trim()</span>
                    }
                </div>
            }
        </div>
    </div>

    <!-- NỘI DUNG -->
    <div class="cf-card">
        <div class="cf-card-head">
            <i class="bi bi-body-text cf-card-icon"></i>
            <span class="cf-card-title">Nội Dung</span>
        </div>
        <div class="cf-card-body">
            <div class="post-content-box">
                @Html.Raw(Model.Content)
            </div>
        </div>
    </div>

    <!-- SEO -->
    @if (!string.IsNullOrEmpty(Model.MetaTitle) || !string.IsNullOrEmpty(Model.MetaDescription) || !string.IsNullOrEmpty(Model.MetaKeywords))
    {
        <div class="cf-card">
            <div class="cf-card-head">
                <i class="bi bi-search cf-card-icon"></i>
                <span class="cf-card-title">Thông Tin SEO</span>
            </div>
            <div class="cf-card-body">
                <div class="seo-row">
                    @if (!string.IsNullOrEmpty(Model.MetaTitle))
                    {
                        <div class="seo-field">
                            <div class="seo-field-label"><i class="bi bi-type-h1"></i> Meta Title</div>
                            <div class="seo-field-val">@Model.MetaTitle</div>
                        </div>
                    }
                    @if (!string.IsNullOrEmpty(Model.MetaDescription))
                    {
                        <div class="seo-field">
                            <div class="seo-field-label"><i class="bi bi-text-paragraph"></i> Meta Description</div>
                            <div class="seo-field-val">@Model.MetaDescription</div>
                        </div>
                    }
                    @if (!string.IsNullOrEmpty(Model.MetaKeywords))
                    {
                        <div class="seo-field">
                            <div class="seo-field-label"><i class="bi bi-tags"></i> Meta Keywords</div>
                            <div class="seo-field-val">@Model.MetaKeywords</div>
                        </div>
                    }
                </div>
            </div>
        </div>
    }

</div>

<!-- ACTIONS -->
<div class="cf-actions">
    <button type="button" class="btn-secondary" data-bs-dismiss="modal">
        <i class="bi bi-x"></i> Đóng
    </button>
    <button type="button" class="btn-primary" onclick="openEditFromDetails(@Model.PostId)">
        <i class="bi bi-pencil"></i> Chỉnh Sửa
    </button>
</div>

<script>
    function openEditFromDetails(id) {
        const currentModal = bootstrap.Modal.getInstance(document.getElementById('modalPost'));
        if (currentModal) currentModal.hide();
        setTimeout(() => { openEditModal(id); }, 300);
    }
</script>

<style>
    .post-content-box h1, .post-content-box h2, .post-content-box h3 {
        font-family: 'Playfair Display',serif;
        color: var(--deep);
    }

    .post-content-box a {
        color: var(--gold);
    }

    .post-content-box blockquote {
        border-left: 3px solid var(--gold);
        padding-left: 1rem;
        color: var(--muted);
        font-style: italic;
    }
</style>
